apiVersion: v1
kind: Service
metadata:
  name: bot
  namespace: vanessa
spec:
  selector: { app: vanessa, component: bot }
  ports: [{ port: 9101, targetPort: metrics, name: metrics }]
---
apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: vanessa
spec:
  selector: { app: vanessa, component: worker }
  ports: [{ port: 9102, targetPort: metrics, name: metrics }]
