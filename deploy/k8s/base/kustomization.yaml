apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  # ConfigMap is scripts/k8s_secrets.py apply — not this list.
  - 20-agent.yaml
  - 21-bot.yaml
  - 22-worker.yaml
  - 23-mcp-servers.yaml
  - 24-metrics-services.yaml
  # NetworkPolicy is opt-in: kubectl apply -f deploy/k8s/base/30-networkpolicy.yaml
