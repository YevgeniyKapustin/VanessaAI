# Vanessa API virtual host — Compose entry point (HTTP).
#
# TLS: copy ssl.conf.example to ssl.conf when this Nginx terminates HTTPS.
# Production Kubernetes typically terminates TLS at the Ingress, not here.

server {
    listen 80;
    server_name _;

    include /etc/nginx/snippets/api_locations.conf;
}
